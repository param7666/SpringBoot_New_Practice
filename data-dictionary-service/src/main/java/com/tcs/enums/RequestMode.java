package com.tcs.enums;

public enum RequestMode {
    DICTIONARY_ONLY,            // dataDictionary + techStack
    DICTIONARY_WITH_WIREFRAME,  // dataDictionary + wireframe + techStack
    DICTIONARY_FULL             // dataDictionary + wireframe + techStack + extraDetails
}
